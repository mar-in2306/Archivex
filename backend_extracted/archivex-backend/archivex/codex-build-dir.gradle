allprojects {
    layout.buildDirectory = file("${rootDir}/build-local")
    afterEvaluate {
        tasks.named("bootRun") {
            classpath += sourceSets.test.runtimeClasspath
        }
    }
}
